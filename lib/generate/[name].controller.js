const restify = require('restify');
const path = require('path');

const utils = require('../utils');
const db = require('../db');
const ${tbl}Service = require('../service/${tbl}.service');

/************************************
** CONTROLLER:   ${tbl}Controller
** AUTHOR:       Unknown
** CREATED DATE: ${createdDate}
*************************************/

${GEN_CONTENT}